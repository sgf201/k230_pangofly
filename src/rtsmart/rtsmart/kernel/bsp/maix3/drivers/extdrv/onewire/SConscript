# RT-Thread building script for component

from building import *

cwd     = GetCurrentDir()
src     = Glob('*.c')
CPPPATH = [cwd]

group = DefineGroup('OneWire', src, depend = ['RT_USING_ONEWIRE'], CPPPATH = CPPPATH)

objs = [group]

Return('objs')
